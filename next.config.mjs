/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['images.rawpixel.com', '5.imimg.com', 'encrypted-tbn0.gstatic.com', 'rukminim2.flixcart.com'], // Add the domain here
    },
  };

export default nextConfig;
